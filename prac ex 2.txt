#include <stdio.h>
#include <unistd.h>

int main()
{
    if (fork() == 0)
        printf("Child 1 PID: %d\n", getpid());
    else if (fork() == 0)
        printf("Child 2 PID: %d\n", getpid());

    return 0;
}
