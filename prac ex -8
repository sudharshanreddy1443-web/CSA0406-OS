#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main()
{
    char data[100];
    int fd = open("student.txt", O_RDONLY);

    read(fd, data, sizeof(data));
    printf("%s", data);

    close(fd);
    return 0;
}
